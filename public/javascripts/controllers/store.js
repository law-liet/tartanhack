(function(){
    var app = angular.module('store', ['book']);

    app.controller('bookController', ['$scope',
        function ($scope){

        }]);

    app.controller('', ['$scope',
        function ($scope) {

        }]);
})();